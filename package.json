{
  "name": "signature-confirm-feishu-plugin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "飞书多维表格签名确认插件上架包",
  "scripts": {
    "pack": "zip -r output.zip build config.json meta.json package.json pnpm-lock.yaml"
  },
  "dependencies": {
    "@lark-base-open/js-sdk": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^4.7.4",
    "vite": "^3.0.4"
  }
}
